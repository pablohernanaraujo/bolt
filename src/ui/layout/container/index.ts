// /src/ui/layout/container/index.ts
// Barrel export file for Container component and related types
// Provides clean imports for the Container layout component
// RELEVANT FILES: container.tsx, types.ts, helpers.ts

export { Container } from './container';
export { buildContainerClassName } from './helpers';
export type { ContainerProps, SimpleContainerProps, SpaceValue } from './types';
