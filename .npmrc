# /Users/pabloaraujo/Documents/projects/ai/design-system/.npmrc
# NPM registry configuration for the Bolt design system package
# Controls package publishing settings and registry behavior
# RELEVANT FILES: package.json, .npmignore, LICENSE, CHANGELOG.md

# Registry configuration
registry=https://registry.npmjs.org/

# Package publishing settings
publish-config-access=public
publish-config-registry=https://registry.npmjs.org/

# Engine strict mode - enforce Node.js version requirements
engine-strict=true

# Save exact versions instead of ranges
save-exact=true

# Use pnpm as package manager
package-manager-strict=true

# Progress bar settings
progress=true

# Audit settings
audit-level=moderate

# Fund messages
fund=false

# Update notifier
update-notifier=false