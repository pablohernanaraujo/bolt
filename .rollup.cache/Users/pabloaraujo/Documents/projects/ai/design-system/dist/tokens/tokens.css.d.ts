export declare const tokens: {
    fonts: {
        body: `var(--${string})`;
        heading: `var(--${string})`;
        mono: `var(--${string})`;
    };
    fontSize: {
        xxs: `var(--${string})`;
        caption: `var(--${string})`;
        xs: `var(--${string})`;
        subtitle: `var(--${string})`;
        sm: `var(--${string})`;
        base: `var(--${string})`;
        lg: `var(--${string})`;
        xl: `var(--${string})`;
        '2xl': `var(--${string})`;
        '3xl': `var(--${string})`;
        '4xl': `var(--${string})`;
        '5xl': `var(--${string})`;
        giant: `var(--${string})`;
    };
    fontWeight: {
        normal: `var(--${string})`;
        medium: `var(--${string})`;
        semibold: `var(--${string})`;
        bold: `var(--${string})`;
    };
    lineHeight: {
        tight: `var(--${string})`;
        normal: `var(--${string})`;
        relaxed: `var(--${string})`;
        loose: `var(--${string})`;
    };
    letterSpacing: {
        tighter: `var(--${string})`;
        tight: `var(--${string})`;
        normal: `var(--${string})`;
        wide: `var(--${string})`;
        wider: `var(--${string})`;
    };
    space: {
        '0': `var(--${string})`;
        '1': `var(--${string})`;
        '2': `var(--${string})`;
        '3': `var(--${string})`;
        '4': `var(--${string})`;
        '5': `var(--${string})`;
        '6': `var(--${string})`;
        '8': `var(--${string})`;
        '10': `var(--${string})`;
        '12': `var(--${string})`;
        '16': `var(--${string})`;
        '20': `var(--${string})`;
        '24': `var(--${string})`;
    };
    radius: {
        none: `var(--${string})`;
        sm: `var(--${string})`;
        base: `var(--${string})`;
        md: `var(--${string})`;
        lg: `var(--${string})`;
        xl: `var(--${string})`;
        '2xl': `var(--${string})`;
        '3xl': `var(--${string})`;
        full: `var(--${string})`;
    };
    zIndex: {
        base: `var(--${string})`;
        dropdown: `var(--${string})`;
        sticky: `var(--${string})`;
        modal: `var(--${string})`;
        popover: `var(--${string})`;
        tooltip: `var(--${string})`;
        toast: `var(--${string})`;
    };
    transition: {
        fast: `var(--${string})`;
        base: `var(--${string})`;
        slow: `var(--${string})`;
    };
    emphasis: {
        high: `var(--${string})`;
        medium: `var(--${string})`;
        low: `var(--${string})`;
        pure: `var(--${string})`;
    };
};
